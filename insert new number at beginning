#include <stdio.h>
int main(void){
    int n=5, a[20] = {1,2,3,4,5};
    int x = 99; // value to insert
    int i;
    for(i=n; i>0; --i) a[i] = a[i-1];
    a[0] = x; n++;
    for(i=0;i<n;i++) printf("%d ", a[i]);
    printf("\n");
    return 0;
}
