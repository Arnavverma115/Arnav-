#include <stdio.h>
int main(void){
    int n=5, a[5],i;
    printf("Enter up to 5 integers (enter count then values):\n");
    scanf("%d",&n);
    for(i=0;i<n;i++) scanf("%d",&a[i]);
    printf("Reversed: ");
    for(i=n-1;i>=0;i--) printf("%d ", a[i]);
    printf("\n");
    return 0;
}
