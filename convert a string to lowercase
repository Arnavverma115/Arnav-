#include <stdio.h>
#include <ctype.h>
int main(void){
    char s[200];
    fgets(s, sizeof s, stdin);
    for(int i=0; s[i]; i++) s[i] = tolower((unsigned char)s[i]);
    printf("%s", s);
    return 0;
}
