#include <stdio.h>
int main(void){
    int a[10] = {1,2,3,4,5}, n=5, pos=2; // remove index 2
    if(pos<0 || pos>=n) { printf("Invalid\n"); return 0; }
    for(int i=pos;i<n-1;i++) a[i] = a[i+1];
    n--;
    for(int i=0;i<n;i++) printf("%d ", a[i]);
    printf("\n");
    return 0;
}
