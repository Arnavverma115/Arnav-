#include <stdio.h>
#include <string.h>
int main(void){
    char s[200];
    fgets(s,sizeof s,stdin); s[strcspn(s,"\n")]=0;
    int n = strlen(s);
    for(int i=n-1;i>=0;i--) putchar(s[i]);
    putchar('\n');
    return 0;
}
